export const sendConfirmationEmail = ({
  to,
  firstName,
}: {
  to: string;
  firstName: string;
}) => {};

export const sendSeminarEmail = ({
  to,
  firstName,
}: {
  to: string;
  firstName: string;
}) => {};
